连接字符串
	名称，连接字符串(加密)
业务配置
	业务：业务id，使用的连接字符串，业务名称
	字段配置：字段key，名称，类型：文本/下拉/复选/文件上传,限制：必填/长度/类型/手机号码/电子邮箱/身份证号码/正则表达式，排序，是否可编辑，主键，是否查询条件，默认值
列表：统一生成-查询，新增按钮
	sql语句配置：配置查询结果的sql语句
	列表配置：行高，表头高度，
	查询条件配置：
		可以直接写html，打开页面时将html直接覆盖对应位置
		动态数据的下拉条件：配置sql，实时查询生成html时自动调用
		可以勾选字段和位置，自动生成html
		分页配置：页码和页大小
		点击查询按钮时，把对应div里的字段按照名称组装请求参数(业务ID，操作,排序，分页信息，参数)发送给后台，后台直接转换为字典值，根据传递的业务ID和业务操作查找对应的sql，解析sql然后调用查询，返回的结果统一为：data，total
	列字段映射；
		配置：列Key，列名，明文/密文，	将查询返回的数据按照列映射，是否排序字段，排序类型(倒序,顺序)，格式，顺序，列宽
		将查询的数据按照列配置进行显示
新增表单：自动生成保存和取消按钮
	sql：配置新增的sql
	表单配置：字段信息，位置
		可以直接写html，打开页面时将html直接覆盖对应位置
		可以勾选字段配置位置，自动生成html
		字段数据源：sql，枚举，items
	点击保存时：先调用统一的表单验证
	
编辑表单
删除

连接数据库
获取表名
选择sql类型
勾选第一个表名和显示的字段
勾选第二个表的关联关系(join,left join,right join)，勾选第二个表名，弹出提示勾选第一个关联字段(左右表格显示列名)并选择关联条件(=,>,<,>=,<=)，勾选第二个关联条件的关系(and or)，勾选第二个关联字段一直到完成关联为止,关联时的字段为动态字段时，可以选择变量@@字段名@@
勾选第三个表的关联关系，勾选第三个表，并弹出2个表供选择第三个表对应的关联表，其他和上面一致
勾选完成关联表后-勾选第一个查询字段，字段的条件(=,>,<,>=,<=,is null,isnot null),填写条件值或者选择变量，选择变量匹配类型:可空，不可空，直接替换
选择第一个排序字段，排序类型，选择第n个排序字段排序类型
选择第一个分组字段，选择第n个分组字段


Generate